// CSES 1662 — Subarray_Sums_II  https://cses.fi/problemset/task/1662
// n <= 2e5, values can be negative. approach: prefix_sum , O(n log n)   (fill BEFORE coding)
#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    ll n, a; cin >> n;
    for (auto i = 0; i < n; i++)

    return 0;
}
